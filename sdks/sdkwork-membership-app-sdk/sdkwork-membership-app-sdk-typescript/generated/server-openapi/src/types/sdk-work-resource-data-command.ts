import type { SdkWorkCommandData } from './sdk-work-command-data';

/** Typed resource data wrapper for command acknowledgement. */
export interface SdkWorkResourceDataCommand {
  item: SdkWorkCommandData;
}
