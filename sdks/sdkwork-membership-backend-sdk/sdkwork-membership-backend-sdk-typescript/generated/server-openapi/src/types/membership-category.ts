/** Membership catalog classification. `token` covers Token Plan compute-credit plans; `community` covers circle/community membership plans. The vocabulary is extensible via migration. */
export type MembershipCategory = 'token' | 'community';
