# Test Fixtures

Test-only seed data. Production bootstrap MUST NOT read from this directory.

- `002_dev_demo.sql`: explicit demo membership, entitlement, reward, and points state for local
  integration tests. It is not referenced by `seeds/seed.manifest.json`.
