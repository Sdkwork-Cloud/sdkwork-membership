# SQLite Migrations

Migration files for SQLite engine. Files MUST use sortable names:

```
0001_description.up.sql
0001_description.down.sql
```

Each numbered migration MUST provide a paired `.down.sql` file.
